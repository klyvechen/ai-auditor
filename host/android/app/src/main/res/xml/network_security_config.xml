<?xml version="1.0" encoding="utf-8"?>
<!-- email-assist's backend defaults to plain HTTP with no HTTPS setup documented (see its
     README); Android 9+ blocks cleartext by default, so this permits it for local addresses
     only. Host-level entitlement per docs/plugin-contract.md's "打包限制" — the module
     itself doesn't declare this. Widen/narrow if a module's backend setup changes. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
